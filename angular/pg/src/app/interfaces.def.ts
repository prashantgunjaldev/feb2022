export interface Course{
    id: number;
    name: string;
    description: string;
}

export interface Contact{
    id: number;
    name: string;
    email: string;
    mobile: string;
    query: string;
}